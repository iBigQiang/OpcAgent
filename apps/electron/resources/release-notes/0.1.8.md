# OPC Agent 0.1.8

### 修复

- 恢复自动化任务列表右上角的新增按钮。新增入口继续使用现有自动化编辑器，并复用普通会话的 `@` Skill 输入能力。
- 修复可拖动的自动化编辑器中 `@` Skill 候选菜单定位偏移的问题。菜单现在始终从当前 `@` 光标位置向上展开，并保留首项选中和滚动浏览。
- Windows 客户端点击窗口右上角 X 时，默认最小化到任务栏，不再直接退出；菜单退出、快捷键退出和更新重启仍按原流程执行。
- Windows 覆盖安装完成页默认勾选并从新安装目录直接启动 OPC Agent，避免依赖尚未就绪的开始菜单快捷方式。
